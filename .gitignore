# Colcon build artifacts
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ROS bag files
*.bag
*.db3

# Backup files
*.backup
*.bak
*~

# Rust / cargo build artifacts
target/

# EPICS autosave runtime state (keep .req config, ignore saved values)
*.sav
*.sav[0-9]*
*.savB

# procServ / IOC runtime logs
*.log

# Rust (robot_sequencer)
src/robot_sequencer/target/

# Hand-eye capture output (handeye.out_dir). The measurements are a
# record of one session on one machine; T_ee_cam is what gets committed.
handeye_samples/

# Taught vision reference frames — site-specific captures, not source.
vision_refs/
# Frames kept by `vision_node --dump`, for analyses written after the cycle
# that produced them. Same reason: a record of this rack on this day.
vision_survey/
