# robot-sequencer configuration.
#
# Paths are resolved relative to this file's directory unless absolute.
# Values mirror the parameters of the replaced ROS node
# (epics_triggered_sequence) and its production launcher
# (desktop/scripts/launch_epics_sequence.sh).

robot:
  ip: "192.168.192.10"           # UR3e; URSim rehearsal: 192.168.56.101
  urdf: "../model/urdf/ur_with_hande_ur3e.urdf"
  srdf: "../model/srdf/ur.srdf"
  mesh_packages:
    ur_description: "../model/meshes/ur_description"
    robotiq_hande_description: "../model/meshes/robotiq_hande_description"
  group: "ur_manipulator"
  ik_frame: "robotiq_hande_end"
  script_file: "../resources/urscript/external_control.urscript"
  output_recipe: "../resources/urscript/rtde_output_recipe.txt"
  input_recipe: "../resources/urscript/rtde_input_recipe.txt"
  rtde_frequency_hz: 50.0        # state reads only; trajectories go out
                                 # over the trajectory interface. Lower is
                                 # safer: unread packages fill the 131 KB
                                 # socket buffer in 0.5 s at 500 Hz and
                                 # URControl drops the client. Does not
                                 # change the servoj period.

# Planning-time joint limit overrides, ported from
# ur3e_hande_moveit_config/config/joint_limits.yaml. URDF carries no
# acceleration limits and TOTG requires them. Position overrides
# (+-pi on the three non-continuous joints) match the MoveIt config;
# the joints MoveIt marked continuous keep the URDF's +-2pi bounds
# (documented deviation: a strict subset of "continuous", and every
# taught waypoint lies inside it).
joint_limits:
  max_velocity: 3.14159          # rad/s, all six arm joints
  max_acceleration: 1.0          # rad/s^2, all six arm joints
  position_overrides:
    shoulder_lift_joint: [-3.14159, 3.14159]
    elbow_joint: [-3.14159, 3.14159]
    wrist_2_joint: [-3.14159, 3.14159]

epics:
  trigger_pv: "Robot:Trigger"
  start_step_pv: "Robot:StartStep"
  wait_pv: "Robot:Wait"
  holder_pv: "Robot:Holder"
  stop_pv: "Robot:Stop"
  current_step_pv: "Robot:CurrentStep"
  gripper_pv: "Robot:Gripper"
  gripper_rbv_pv: "Robot:Gripper_RBV"
  pause_step_pv: "Robot:PauseStep"
  calib_mode_pv: "Robot:CalibMode"
  loaded_pv: "Robot:Loaded"
  jog_x_pv: "Robot:JogX"
  jog_y_pv: "Robot:JogY"
  jog_z_pv: "Robot:JogZ"
  jog_step_pv: "Robot:JogStep"

sequence:
  waypoints_yaml: "taught_waypoints.yaml"
  holder_offset: 0.03            # m between holders in Y
  velocity_scale: 1.0
  acceleration_scale: 1.0
  cartesian_translation_step: 0.005   # m, 5 mm interpolation resolution
  cartesian_rotation_step: 0.05       # rad
  cartesian_min_fraction: 0.95        # below this: fall back to planned move
  jog_velocity_scale: 0.1             # calibration jog runs slow

  # Keep the gripper level through joint-space plans (steps 7 and 18).
  # Those two only constrain the goal, so the planner is free to roll the
  # tool over on the way; measured on the robot, the 63-degree holder-to-
  # stage move swung it to vertical. Cartesian steps interpolate the pose
  # and never had the problem.
  #
  # The constraint is "approach axis stays horizontal", not "orientation
  # stays fixed": the holder is approached along -Y and the stage along
  # +X, 92 degrees apart, so rotation within the horizontal plane must
  # stay free. The taught poses are themselves 1.64 and 0.84 degrees off
  # level, leaving ~1.4 degrees of margin at 3.0.
  level_tool:
    enabled: true
    tolerance_deg: 3.0

gripper:
  # "hande": Robotiq Hand-E over UR tool communication (robot ip:54321).
  # "simulated": no hardware; position tracks commands instantly
  # (URSim has no RS485 device behind 54321).
  mode: "hande"
  open_position: 0.025           # m, open command
  close_position: 0.0            # m, close command (production closes via
                                 # SRDF named target "close" = 0.0; the
                                 # gripper stalls on a gripped sample)
  close_settle_target: 0.01      # m, settle-wait target for close
                                 # (reference gripper_close_position)
  reach_tolerance: 0.0015        # m
  settle_timeout: 5.0            # s
  open_threshold: 0.02           # m, Gripper_RBV open/close boundary
  poll_hz: 50                    # Hand-E background comm rate. The tool-
                                 # comm bridge drops ~1 transaction in a
                                 # few thousand (measured); each cycle is
                                 # two, so a lower rate means fewer stalls.
                                 # The driver also retries a lost one.

# Static collision scene, replacing stage_scene_utils + setup_stage_acm.
#
# The stage is an approximate convex decomposition of resources/stage.stl,
# built by tools/stage_collision (see its module doc). The CAD mesh has
# 231,812 triangles and cost ~150 s of BVH build per daemon start and
# ~930 ms per joint-space plan; these 40 convex parts cost neither
# measurably. They contain the original shape, so they can only report
# more collisions than it, never fewer — the price is that thin
# concavities fill in, and holder1_on_position now reads as a collision.
# Nothing plans into it (the steps that reach it are Cartesian), but a
# joint-space plan into a slot would fail. Regenerate with:
#   cargo run --release --manifest-path ../tools/stage_collision/Cargo.toml \
#     -- ../resources/stage.stl ../resources/stage_parts
scene:
  objects:
    - { id: "stage0", stl: "../resources/stage_parts/stage_part00.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage1", stl: "../resources/stage_parts/stage_part01.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage2", stl: "../resources/stage_parts/stage_part02.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage3", stl: "../resources/stage_parts/stage_part03.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage4", stl: "../resources/stage_parts/stage_part04.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage5", stl: "../resources/stage_parts/stage_part05.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage6", stl: "../resources/stage_parts/stage_part06.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage7", stl: "../resources/stage_parts/stage_part07.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage8", stl: "../resources/stage_parts/stage_part08.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage9", stl: "../resources/stage_parts/stage_part09.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage10", stl: "../resources/stage_parts/stage_part10.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage11", stl: "../resources/stage_parts/stage_part11.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage12", stl: "../resources/stage_parts/stage_part12.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage13", stl: "../resources/stage_parts/stage_part13.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage14", stl: "../resources/stage_parts/stage_part14.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage15", stl: "../resources/stage_parts/stage_part15.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage16", stl: "../resources/stage_parts/stage_part16.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage17", stl: "../resources/stage_parts/stage_part17.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage18", stl: "../resources/stage_parts/stage_part18.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage19", stl: "../resources/stage_parts/stage_part19.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage20", stl: "../resources/stage_parts/stage_part20.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage21", stl: "../resources/stage_parts/stage_part21.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage22", stl: "../resources/stage_parts/stage_part22.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage23", stl: "../resources/stage_parts/stage_part23.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage24", stl: "../resources/stage_parts/stage_part24.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage25", stl: "../resources/stage_parts/stage_part25.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage26", stl: "../resources/stage_parts/stage_part26.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage27", stl: "../resources/stage_parts/stage_part27.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage28", stl: "../resources/stage_parts/stage_part28.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage29", stl: "../resources/stage_parts/stage_part29.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage30", stl: "../resources/stage_parts/stage_part30.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage31", stl: "../resources/stage_parts/stage_part31.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage32", stl: "../resources/stage_parts/stage_part32.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage33", stl: "../resources/stage_parts/stage_part33.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage34", stl: "../resources/stage_parts/stage_part34.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage35", stl: "../resources/stage_parts/stage_part35.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage36", stl: "../resources/stage_parts/stage_part36.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage37", stl: "../resources/stage_parts/stage_part37.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage38", stl: "../resources/stage_parts/stage_part38.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
    - { id: "stage39", stl: "../resources/stage_parts/stage_part39.stl",
        scale: [0.01, 0.01, 0.01], position: [-0.15, 0.39, -0.002], rpy: [0.0, 0.0, 3.14159] }
  # setup_stage_acm.py allowances: collisions between these links and
  # every scene object are ignored.
  allow_collisions_with:
    - "base_link"
    - "base_link_inertia"
    - "base"
    - "world"

# Vision look-then-move fine correction (doc/vision_camera_setup.md).
# Off until the wrist camera + vision node are commissioned; when
# enabled, every Robot:Vision PV must be served and answered, and a
# failed/invalid/over-limit measurement stops the sequence (resume via
# StartStep as with any step failure). observe_only is Phase C: measure
# and log at every hook, never move, never fail.
vision:
  enabled: false
  observe_only: false
  timeout: 2.0            # s to wait for the vision node's answer
  # The deadband filters the arm, not the sensor: 20 production cycles put
  # the re-approach spread at the observation pose at sigma (0.022, 0.007) mm
  # and peak-to-peak (0.095, 0.026) mm, against an estimator floor of
  # 0.001 mm (doc §14). 0.10 sits above the observed spread; the old 0.05
  # sat inside it, so 1 cycle in 20 would have been corrected for nothing.
  min_correction: 0.10    # mm, deadband below which corrections are noise
  max_correction: 3.0     # mm, larger corrections stop the sequence
  # Each hook observes from a standby pose, where the target is near the
  # middle of the frame, and the correction is applied to the above/on
  # pair that follows.
  #
  # The two that are off are off because tools/vision/vision_node.py does
  # not measure them and answers Valid=0 rather than zeros — zeros would
  # read as "aligned" and "seated" and be indistinguishable from a real
  # answer. grip_offset looks at the puck in the fingers, which rides
  # 33 deg off the optical axis with only its near rim inside the frame;
  # seating needs a tilt, and depth on this metal is 1.83 mm biased and
  # 8.48 mm spread. Turning either on without teaching the node how to
  # answer it stops the sequence every cycle, which is the correct
  # failure and not a reason to make the node invent a number.
  pick_align: true        # at steps 1 and 12, applied to 2/3 and 13/14
  grip_offset: false      # at steps 5 and 16 — not measured (§13.2)
  place_align: true       # at steps 7 and 18, applied to 8/9 and 19/20
  seating_check: false    # at steps 12 and 23 — not measured (§12.1)

# Eye-in-hand calibration capture (CalibMode = 3). Commissioning only:
# it produces the T_ee_cam that the vision node above needs before it can
# turn pixels into a TCP-local correction at all. Two triggers: the first
# enters an aiming hold where JogX/Y/Z move the TCP and the tag position
# is logged once a second, the second starts the capture from wherever
# the arm then is. The arm rotates the tool about its own axes, samples
# the tag at each stop, and returns to where it started. Solve with:
#   <solve_python> tools/handeye/solve_joint.py <out_dir>/samples_<timestamp>.yaml
# (the capture logs the exact path it wrote; each one gets its own name so
# a second capture cannot quietly replace the first)
handeye:
  # Two interpreters because no one environment here has both halves: the
  # detector needs pyepics to reach the camera's PVs, the solver needs
  # yaml and scipy, and the pydm env has no yaml.
  python: "/home/bl9b/micromamba/envs/pydm/bin/python3.11"
  solve_python: "/usr/bin/python3"
  detector: "../tools/handeye/detector.py"
  out_dir: "../handeye_samples"
  angle_deg: 16.0         # largest tool rotation; bounded by the tag staying in frame.
                          # 8 deg left t_ee_cam underdetermined: (R_a - I) inverts the
                          # detection noise into the translation, so the methods spread
                          # 2.9 mm while agreeing to 0.03 deg on rotation. 16 halves that
                          # amplification. The ceiling is rx, which walks the tag 5.5 px
                          # per degree down a 480 px frame.
  standoff_mm: [0.0, -50.0, -100.0]
                          # the rotation set repeats at 307 and 357 mm as well as the
                          # aiming 257 mm. At one distance the tag's solvePnP depth error
                          # is the same in every sample and reads as camera mount offset:
                          # the 16 deg run agreed to 2.0 mm between methods while leaving
                          # one out moved the answer 5.6 mm. Only away from the tag —
                          # reproj tracks the tag's pixel size (0.24 px at 128 px, 0.99 at
                          # 174), which is a geometric error on the sheet rather than
                          # detection noise, so closing in makes every sample worse: the
                          # 227 mm group alone spread 7.55 mm against 1.41 at 307 mm.
  velocity_scale: 0.1     # slower than the sequence: every pose is a fresh excursion
  min_samples: 5          # below this the capture is reported failed, not written

# Force-stopped seat probing (CalibMode=5). Two triggers: the first opens a
# jog hold, the second probes from wherever the arm was jogged to. It prints
# where the bore is and writes nothing.
#
# Every number here bounds how hard the arm may push on a sample, so they are
# validated at startup rather than at the trigger.
probe:
  velocity_scale: 0.02    # each step is a move into something that may be there
  loosen_mm: 1.2          # play opened before stepping. A gripped, seated puck
                          # closes the gripper-puck-bore loop and every direction
                          # saturated on the first 0.05 mm step (1.2/1.5/6.3 N,
                          # 2026-08-15) with nothing free in front of it. Half of
                          # this is the free run before a pad touches the puck
                          # again, so it has to beat the nominal 0.50 mm radial
                          # clearance: at 0.3 the free run was 0.2 mm and the
                          # first-step trips could not be attributed.
  lateral:                # sideways, toward a bore wall
    step_mm: 0.05         # ten steps to the nominal 0.50 mm radial clearance,
                          # and the overshoot past first contact is one of them
    travel_mm: 1.5        # past the clearance, so "no contact" means the bore is
                          # not where the pose says rather than the probe was short
    threshold_n: 0.5      # 7x the 0.073 N the arm scatters standing still; reading
                          # force between steps rather than during them is what
                          # makes a threshold this low usable (doc §16.1)
    abort_n: 5.0          # a rubbing insert was measured at 8-23 N. That is the
                          # force this mode exists to stop the sequence applying,
                          # not a level to probe up to.
  depth:                  # downward, toward the seat floor
    step_mm: 0.10
    travel_mm: 4.0
    threshold_n: 1.0      # the puck's own weight is already on the floor
    abort_n: 8.0
